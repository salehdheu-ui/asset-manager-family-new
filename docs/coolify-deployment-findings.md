
## فحص السجلات

- سجل التطبيق يعرض طلبات API ناجحة للمصادقة ولوحة التحكم والتقارير.
- توجد رسالة خطأ دورية من عامل الإشعارات أثناء `getDueNotifications` داخل `Timeout`، لكن نص الاستعلام مقطوع في واجهة السجل ولا يكفي لتحديد السبب.
- آخر سجل مرئي يشير إلى أن التطبيق يعمل على build منشور سابق، وليس على التغييرات المحلية الحالية.
- لم يتم فتح Environment Variables أو تعديلها لتجنب كشف أو تغيير الأسرار.

محاولة فتح مسار `/deployment` مباشرة أعادت المتصفح إلى لوحة Coolify الرئيسية بدل صفحة التطبيق؛ يلزم استخدام رابط الصفحة المستخرج من الواجهة أو إعادة فتح التطبيق ثم اختيار deployments من داخله. لم يتم تنفيذ أي تغيير.

ظهر 404 عند استخدام معرف مشروع مختصر بالخطأ. معرف المشروع الصحيح في الرابط الأصلي هو `vg04ck4swgs884kgo0cgckgo`، وليس `vg04ck4swgs884kgo`. لم يتم تعديل البيئة.

أعيد فتح التطبيق بالمعرف الكامل الصحيح وظهر التطبيق بحالة `Running`. صفحة Configuration تؤكد أن فرع Git هو `main` وأن الـ commit المنشور ما زال `2c527d0`. رابط deployments الصحيح داخل الواجهة هو `/project/vg04ck4swgs884kgo0cgckgo/environment/xc00sk4sk8sowgw0g44og0k0/application/lk40g8kc00s48gcwk8wko00c/deployment`، بينما فتح نسخة ناقصة من المعرف أدى إلى 404. لم يتم الضغط على Redeploy أو Save.

بعد فتح الرابط الصحيح، ظهرت صفحة configuration بحالة `Running` والـ commit الظاهر ما زال `2c527d0`. لم يبدأ نشر جديد في هذه الخطوة؛ السبب المرجح أن التعديلات المحلية لم تُرفع إلى GitHub بعد، لذلك لا يمكن لـ Coolify التقاطها من الفرع `main`. لم يتم الضغط على Redeploy فعليًا في هذه الجولة بسبب بقاء commit قديم ظاهرًا.

بعد رفع commit `34233df` إلى GitHub وتشغيل Redeploy المؤكد، أنشأ Coolify deployment جديدًا بالمعرف `x13x560hg7wrr5h3xspbyc9r`، وحالته الحالية `Deployment is Queued` مع `No logs yet`. لم أضغط Cancel أو Force Start؛ سأنتظر بدء العامل وأتحقق من السجل.

التقط Coolify commit `34233df` بنجاح وبدأ deployment جديدًا. السجل يوضح أن Nixpacks اكتشف Node على الإصدار 22.12.0، وأن بناء صورة Docker بدأ دون خطأ حتى آخر تحديث مرئي. إعداد البناء الحالي ما زال يشغّل `npm install && npm run db:push && npm run build`.

في آخر متابعة، ما زال deployment `x13x560hg7wrr5h3xspbyc9r` بحالة `In Progress`. لم يظهر خطأ، لكن سجل الواجهة لم يتقدم بعد سطر `Building docker image started`، لذلك لم يتم اعتبار النشر مكتملًا بعد.

بعد متابعة إضافية، ظل deployment بحالة `In Progress` دون سطور جديدة بعد `Building docker image started`. سأفحص Debug Logs للحصول على تفاصيل البناء بدل تكرار الانتظار العام.

بعد فتح Debug Logs ظهر تقدم فعلي في BuildKit: تحميل build context بحجم 6.87MB، تجهيز صورة Nixpacks، وبدء مراحل بناء Docker بنجاح. السجل يؤكد أن Coolify يستخدم commit `34233dfa2fba514fc3d5cce6f780b3e47278f234` وقرأ رسالة الـ commit الجديدة. لا يوجد خطأ حتى آخر سطر مرئي.

السجل التفصيلي يوضح استمرار البناء عبر مراحل Docker/Nixpacks، مع تحميل build context ونسخ ملفات `.nixpacks` بنجاح. لم تظهر رسالة فشل أو توقف صريح حتى آخر تحديث مرئي، وما زالت العملية `In Progress`.

آخر Debug Logs تعرض تقدم Nixpacks في طبقات النظام (`stage 8/16`) وتنظيف حزم `/nix/store`، ما يدل على أن البناء مستمر وليس عالقًا في التطبيق أو قاعدة البيانات. لا يوجد خطأ ظاهر حتى آخر تحديث، والعملية ما زالت `In Progress`.

تقدمت عملية البناء: اكتملت مرحلة تنظيف/تجهيز Nixpacks (`4/16`) خلال 112.4 ثانية، ثم بدأت مرحلة نسخ ملفات nixpacks التالية (`5/16`) بنجاح. لا توجد أخطاء في السجل حتى آخر تحديث، لكن البناء لم يكتمل بعد.

آخر فحص يؤكد استمرار البناء في Coolify؛ تظهر مراحل Docker حتى `5/16`، ولا توجد رسالة فشل. التطبيق القديم ما زال ظاهرًا كـ Running أثناء البناء، وهو سلوك متوقع حتى يكتمل النشر الجديد.

في آخر تحديث مرئي، بقي deployment في حالة `In Progress`، وسجل Debug Logs عند مراحل Nixpacks الوسيطة بعد إكمال المرحلة الرابعة. لا يوجد خطأ في البناء، لكن الخدمة الجديدة لم تبدأ بعد.

حتى آخر فحص، deployment ما زال `In Progress` وسجل Debug Logs عند مراحل Nixpacks المتوسطة؛ لا يظهر فشل. استمر التطبيق القديم في الخدمة أثناء تجهيز الصورة الجديدة، ولم تُجرَ أي عملية إلغاء أو تشغيل قسري.

آخر Debug Logs تُظهر أن البناء وصل إلى مرحلة Docker `7/16`، مع نسخ حزم Nix من cache وبدء تجهيز Node/npm. لا يوجد خطأ، والسرعة طبيعية نسبيًا لمرحلة بناء الصورة.

تقدم البناء إلى مرحلة تثبيت حزم Ubuntu داخل الصورة، وبدأت قراءة قوائم الحزم والاعتماديات بنجاح. لا توجد رسالة فشل حتى آخر Debug Log، وما زال deployment قيد التنفيذ.

وصل Coolify إلى مرحلة `npm install` داخل الصورة الجديدة. ظهرت تحذيرات `EBADENGINE` لأن بعض الحزم تطلب Node `>=22.12.0` بينما البيئة الحالية `22.11.0`، إضافة إلى تحذيرات حزم deprecated؛ هذه تحذيرات غير قاتلة حتى السطر المرئي، لكن يوصى لاحقًا بتثبيت Node 22.12+ ومراجعة الاعتماديات.

وصل deployment إلى `npm run db:push` وظهرت رسالة `Changes applied` بنجاح من Drizzle، ثم بدأ `npm run build`. هذا يؤكد أن أعمدة Object Storage في schema طُبقت على قاعدة بيانات البيئة بنجاح. ظهرت ملاحظة npm باستخدام `--omit=dev` مستقبلًا، لكنها ليست فشلًا.

أكد السجل أن `db:push` انتهى برسالة `Changes applied`، ثم بدأ `npm run build`. آخر تحديث ما زال في مرحلة build دون فشل، لذلك لم تُجرَ أي تغييرات إضافية في Coolify.

انتهى deployment بنجاح (`Deployment is Finished`). npm build اكتمل خلال 20.54 ثانية، وأُنشئت الصورة المرتبطة بالـ commit `34233dfa2fba514fc3d5cce6f780b3e47278f234`. بقي تحذير معروف بأن `dist/index.cjs` بحجم يقارب 1.2MB وأن `exceljs.min` يقارب 937KB؛ التحذير لا يمنع التشغيل.

اختبار ما بعد النشر نجح: `https://family.saher.cloud` يعرض صفحة الدخول العربية والواجهة العامة بشكل صحيح. كما أعاد `GET /api/auth/user` الاستجابة العربية `غير مصرح` لغير المسجلين، وهي استجابة المصادقة المتوقعة وليست خطأ تشغيل.

المسار `/platform-review` لا يكشف التقرير للزائر غير المسجل؛ أعاد المستخدم إلى صفحة الدخول، ما يؤكد أن الحماية الإدارية تعمل في النسخة المنشورة. ظهرت أيضًا دعوة تثبيت PWA في الصفحة العامة.
